import { tv } from 'tailwind-variants';

export const checkboxRootTV = tv({
  base: 'ring-1 ring-placeholder flex h-4 w-4 appearance-none items-center justify-center outline-none'
});

export const checkboxWrapperTV = tv({
  base: 'flex items-center gap-2'
});
